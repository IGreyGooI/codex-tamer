interface:
  display_name: "Codex Tamer"
  short_description: "Share and control Codex app-server threads"
  default_prompt: "Use $codex-tamer to reuse or start the shared Codex app-server, then inspect and safely control the relevant thread."
